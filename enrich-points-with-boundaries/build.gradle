version = '1.0.0'

allprojects {
	repositories {
		mavenCentral()
	}
}

subprojects {
	tasks.withType(Jar) {
		archivesBaseName = "${project.parent.name}-${project.name}"
	}
}

wrapper {
	gradleVersion = "6.4.1"
}